struct Cam{
	VECTOR pos;
	VECTOR rot;
} Cam;

//struct Cam CamInit(VECTOR pos, VECTOR rot) {
//	struct Cam cam;
//
//	memcpy(cam.pos, pos, sizeof(pos));
//	memcpy(cam.rot, rot, sizeof(rot));
//
//	return cam;
//}
